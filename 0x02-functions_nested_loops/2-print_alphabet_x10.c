#include "main.h"

/**
 * print_alphabet_x10: function to print a-z 10times
 *
 * Return: 0
 */
void print_alphabet_x10(void);
{
	char l, j;

	for (l = 0; l <= 9; l++)
	{
		for (j = 'a'; j <= 'z'; j++)
		}
			_putchar(j);
		}
	}
		_putchar('\n');
}
