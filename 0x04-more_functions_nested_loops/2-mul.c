#include "main.h"

/**
 * mul - Function to multiply two numbers
 * @a: first number
 * @b: second number
 * Return: Always 0
 */
int mul(int a, int b)
{
	int result;

	result = (a * b);
	return (result);

	return (0);
}
