#include "main.h"
#include <stdio.h>

/**
 * *_strcat - Function to concate two strings
 * @dest: string one
 * @src: string two
 *
 * Return: Always 0
 */
char *_strcat(char *dest, char *src)
{
	int n, i;

	for (i = 0; dest[i] != '\0'; i++)
	{
	}

	for (n = 0; (dest[i + n] = *src++) != '\0'; n++)
	{
	}
	return (dest);
}
